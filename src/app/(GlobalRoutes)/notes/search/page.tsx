import type {Metadata} from "next";
export const metadata: Metadata = {
    title: "Search",
    description: "Note Taking App Search Page"
};
export default function SearchPage() {
    return (
        <>
            <h1>search</h1>
        </>
    );
}
