import type {Metadata} from "next";
export const metadata: Metadata = {
    title: "Settings",
    description: "Note Taking App | Setting Page For fonts, theme, and change password"
};
export default function Settings() {
    return <></>;
}
